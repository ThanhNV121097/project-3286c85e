# Runtime PostgreSQL connection string. Example: postgres://app:app_secret@localhost:5432/app?sslmode=disable
DATABASE_URL=

# HTTP listen port. Runtime injects this value.
PORT=8080

# Optional fallback HTTP listen port when PORT is absent.
APP_PORT=8080
